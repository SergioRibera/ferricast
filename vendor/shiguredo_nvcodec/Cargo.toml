# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.88"
name = "shiguredo_nvcodec"
version = "2026.1.0"
authors = ["Shiguredo Inc."]
build = "build.rs"
include = [
    "/src/**",
    "/build.rs",
    "/third_party/**",
    "LICENSE",
    "README.md",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Rust bindings for NVIDIA Video Codec SDK"
homepage = "https://github.com/shiguredo/nvcodec-rs"
readme = "README.md"
license = "Apache-2.0"
repository = "https://github.com/shiguredo/nvcodec-rs"

[package.metadata.external-dependencies.nvcodec]
version = "13.0.19"

[lib]
name = "shiguredo_nvcodec"
path = "src/lib.rs"

[dependencies]

[build-dependencies.bindgen]
version = "0.72"

[build-dependencies.shiguredo_toml]
version = "2026.1"
